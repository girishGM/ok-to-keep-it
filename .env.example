# Copy to .env and adjust. .env is git-ignored and never committed.
# Port the web server binds to. Render sets this automatically.
PORT=8000
# Node version Render uses to build the React frontend (optional; Render default otherwise).
NODE_VERSION=22
